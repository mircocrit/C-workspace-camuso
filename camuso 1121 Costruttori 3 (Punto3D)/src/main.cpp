#include <iostream>
#include <string>
#include "punto3D.h"
using namespace std;


int main(){
   // string s="stringa_prova"; //deve diventare: Stringa prova
   // s.replace(0,1,"S").replace(7,1," ");
   // cout << s << endl;
    punto3D p(3,4,5);
    p.getX().getX();
    p.getXp()->getXp();
    system("pause");
    return 0;
}
